我们知道http是无状态的。

**无状态**——不保存状态。HTTP协议自身不具备保存之前发送过的请求或响应的功能。之所以这样设计是为了能快速的处理大量事务，确保协议的课伸缩性。

如何解决不能保存状态：cookie和session，cookie是身份证的话那么session就是档案表

访问量大的时候cookie会占用带宽

但是session又会遇到分布式问题——不容易在多台服务器之间共享。

